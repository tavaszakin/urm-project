export const DEMO_ROUTE = "/demo";

export const DEMO_FUNCTION_SPEC = { kind: "add" };

export const DEMO_INPUTS = [2, 3];

export const DEMO_TITLE = "See Computation Step by Step";

export const DEMO_SUBTITLE = "Explore how simple functions execute as register machines.";

export const DEMO_EXPLANATION = [
  "This demo starts with addition and prefilled inputs so the machine is doing real work immediately.",
  "Each trace row is one execution step. The yellow row marks the current step, and pink cells mark the registers that changed at that step.",
  "Use Play, Prev, Next, or the slider to scrub through the computation and watch the URM program update alongside the trace.",
];
